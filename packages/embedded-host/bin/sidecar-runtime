#!/bin/sh
# The runtime the process provisioner spawns a sidecar with. No `intx-src`
# condition here: the sidecar evaluates deployed workflow packages whose
# published `@intx/*` dependencies ship only `dist/`, and the vendored
# packages have their own `dist/` from `vendor:build`, so both resolve alike.
exec bun "$@"
