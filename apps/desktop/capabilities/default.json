{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "The window loads the host's loopback URL in local mode, or SOLUTIONS_BUILDER_HUB_URL in remote mode. That origin is the page: it needs the IPC to ask the shell for dictation and to hear its events, and to print: on macOS the webview's window.print is the shell's own print command.",
  "windows": [
    "main"
  ],
  "permissions": [
    "core:default",
    "core:webview:allow-print",
    "allow-dictation-start",
    "allow-dictation-stop",
    "allow-dictation-open-settings",
    "allow-start-at-login",
    "allow-set-start-at-login",
    "allow-quit-app"
  ],
  "remote": {
    "urls": [
      "http://127.0.0.1:*",
      "http://localhost:*",
      "https://*"
    ]
  }
}
