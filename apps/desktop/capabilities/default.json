{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "The window loads the host's loopback URL, so that origin is the page: it needs the IPC to ask the shell for dictation and to hear its events.",
  "windows": [
    "main"
  ],
  "permissions": [
    "core:default",
    "allow-dictation-start",
    "allow-dictation-stop",
    "allow-dictation-open-settings"
  ],
  "remote": {
    "urls": [
      "http://127.0.0.1:*",
      "http://localhost:*"
    ]
  }
}
