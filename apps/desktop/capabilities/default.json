{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "The window loads the host's loopback URL directly; no extra IPC surface is needed.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
